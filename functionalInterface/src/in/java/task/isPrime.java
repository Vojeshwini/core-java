package in.java.task;
//Create a functional interface for checking if a number is prime. Use a lambda to test multiple numbers.

@FunctionalInterface
public interface isPrime {
    void checkIsPrime(int number);

}
